package Geometry;

public class Rectangle {
	
	private Point upperLeft;
	private Point lowerDown;
	Rectangle(Point p1, Point p2) {
		if (p1.getX() == p2.getX() || p1.getY() == p1.getY()) {
			
		}
		//upperLeft = new Point(p1);
	}
}
